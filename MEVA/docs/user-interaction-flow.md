# MEVA 사용자 인터랙션 플로우

## 문서 정보
- **작성일**: 2025-11-17
- **프로젝트**: MEVA (Materials Engineering Visualization and Analysis)
- **버전**: 1.0
- **관련 요구사항**: UI-GUI-001, UI-GUI-002, UI-GUI-003, NF-USE-001

---

## 1. 개요

### 1.1 목적
본 문서는 MEVA 애플리케이션의 사용자 인터랙션 플로우를 정의합니다. 재료공학 학부생이 직관적으로 사용할 수 있도록 설계된 주요 사용자 시나리오와 인터랙션 패턴을 기술합니다.

### 1.2 대상 사용자
- 재료공학과 학부생
- 재료 물성 데이터 분석이 필요한 연구자
- 인장시험 결과 시각화가 필요한 사용자

---

## 2. 주요 사용자 시나리오

### 2.1 시나리오 1: 새로운 재료 시뮬레이션 수행

#### 목표
사용자가 재료 물성값과 시편 치수를 입력하여 응력-변형률 곡선을 생성하고 주요 물성값을 계산합니다.

#### 사용자 플로우
```
애플리케이션 시작
  ↓
메인 화면 표시
  ↓
재료 물성값 입력 (영계수율, 항복강도, 인장강도 등)
  ↓
시편 치수 입력 (길이, 넓이, 두께)
  ↓
[Calculate] 버튼 클릭
  ↓
입력값 검증
  ↓ (유효)
계산 진행 (StatusBar에 진행률 표시)
  ↓
응력-변형률 그래프 표시
  ↓
계산된 물성값 ResultsTable에 표시
  ↓
완료 메시지 표시
```

#### 상세 인터랙션

**Step 1: 재료 물성값 입력**
- 위치: InputPanel > MaterialPropertiesPanel
- 입력 필드:
  - Young's Modulus (E): 기본값 200 GPa
  - Yield Strength (σy): 기본값 250 MPa
  - Tensile Strength (σu): 기본값 400 MPa
  - Strength Coefficient (K): 기본값 500 MPa
  - Strain Hardening Exponent (n): 기본값 0.2
- 사용자 액션:
  - 각 필드에 숫자 입력
  - Tab 키로 다음 필드로 이동
  - 단위 콤보박스에서 단위 선택 (선택적)
- 시스템 반응:
  - 입력 중 실시간 검증 (숫자만 허용)
  - 유효하지 않은 입력 시 빨간색 테두리 표시
  - 포커스 이탈 시 범위 검증
  - 툴팁으로 허용 범위 안내

**Step 2: 시편 치수 입력**
- 위치: InputPanel > SpecimenDimensionsPanel
- 입력 필드:
  - Width (W): 기본값 10 mm
  - Thickness (t): 기본값 5 mm
  - Length (L): 기본값 50 mm
  - Gauge Length: 기본값 25 mm
- 사용자 액션:
  - 각 필드에 치수 입력
  - 단면적(W × t)은 자동 계산되어 표시됨
- 시스템 반응:
  - 입력값 변경 시 단면적 즉시 재계산
  - 유효하지 않은 치수 입력 시 경고

**Step 3: 계산 실행**
- 위치: InputPanel > ControlPanel
- 사용자 액션:
  - [Calculate] 버튼 클릭 또는 F5 단축키
- 시스템 반응:
  1. 모든 입력값 검증
  2. 유효하지 않은 입력 시:
     - 오류 다이얼로그 표시
     - 문제가 있는 필드로 포커스 이동
     - StatusBar에 오류 메시지 표시
  3. 유효한 입력 시:
     - StatusBar: "Calculating..." 표시
     - ProgressBar 활성화
     - [Calculate] 버튼 비활성화
     - 백그라운드에서 계산 수행

**Step 4: 결과 표시**
- 위치: VisualizationPanel, ResultsPanel
- 시스템 반응:
  1. 응력-변형률 곡선 그래프 생성
     - 탄성 구간 (선형)
     - 항복 구간 (항복점 마커 표시)
     - 소성경화 구간 (곡선)
     - 인장강도 지점 (최대값 마커 표시)
  2. ResultsTable에 계산된 물성값 표시:
     - Young's Modulus (E)
     - Yield Strength (σy)
     - Ultimate Tensile Strength (σu)
     - Elongation (%)
     - Reduction of Area (%)
     - Fracture Strain
  3. StatusBar: "Calculation completed" 표시
  4. [Calculate] 버튼 재활성화

#### 예상 소요 시간
- 입력: 30초 ~ 1분
- 계산 및 결과 표시: < 1초 (NF-PERF-001)

---

### 2.2 시나리오 2: 프리셋 재료 사용

#### 목표
미리 정의된 재료 물성값을 불러와 빠르게 시뮬레이션을 수행합니다.

#### 사용자 플로우
```
메인 화면
  ↓
PresetPanel > MaterialPresetComboBox 클릭
  ↓
재료 선택 (예: Steel, Aluminum, Copper)
  ↓
[Load Preset] 버튼 클릭
  ↓
선택한 재료의 물성값 자동 입력
  ↓
시편 치수만 추가 입력 (필요 시)
  ↓
[Calculate] 버튼 클릭
  ↓
결과 표시
```

#### 상세 인터랙션

**Step 1: 프리셋 선택**
- 위치: InputPanel > PresetPanel
- 사용자 액션:
  - MaterialPresetComboBox 드롭다운 클릭
  - 목록에서 재료 선택:
    - Steel (Default)
    - Aluminum
    - Copper
    - Titanium
    - Custom
- 시스템 반응:
  - 선택한 재료 하이라이트
  - [Load Preset] 버튼 활성화

**Step 2: 프리셋 로드**
- 사용자 액션:
  - [Load Preset] 버튼 클릭
- 시스템 반응:
  - 데이터베이스에서 재료 물성값 조회 (DB-DATA-002)
  - MaterialPropertiesPanel의 모든 필드 자동 채우기:
    - 애니메이션 효과 (0.3초)
    - 각 필드가 순차적으로 업데이트됨을 시각화
  - StatusBar: "Preset loaded: [재료명]" 표시
  - 확인 다이얼로그 (선택적): "Steel properties loaded successfully"

**Step 3: 추가 입력 및 계산**
- 시나리오 1과 동일한 프로세스

#### 지원 프리셋 목록

| 재료 | E (GPa) | σy (MPa) | σu (MPa) | K (MPa) | n |
|------|---------|----------|----------|---------|-----|
| Steel | 200 | 250 | 400 | 500 | 0.2 |
| Aluminum | 70 | 95 | 110 | 180 | 0.15 |
| Copper | 120 | 70 | 220 | 315 | 0.54 |
| Titanium | 110 | 880 | 950 | 1100 | 0.05 |

---

### 2.3 시나리오 3: 실험 데이터 불러오기

#### 목표
 CSV 또는 Excel 파일에서 실험 데이터를 불러와 시뮬레이션을 수행합니다.

#### 사용자 플로우
```
메인 화면
  ↓
File > Open 메뉴 클릭 또는 Ctrl+O
  ↓
FileChooser 다이얼로그 표시
  ↓
파일 선택 (CSV/Excel)
  ↓
[Open] 버튼 클릭
  ↓
파일 파싱 및 검증
  ↓ (유효)
입력 필드 자동 채우기
  ↓
그래프 자동 생성 (선택적)
  ↓
결과 표시
```

#### 상세 인터랙션

**Step 1: 파일 선택**
- 사용자 액션:
  - 메뉴: File > Open (Ctrl+O)
  - 또는 ToolBar의 [Open] 버튼 클릭
  - 또는 InputPanel의 [Import Data] 버튼 클릭
- 시스템 반응:
  - JFileChooser 다이얼로그 표시
  - 파일 필터 설정:
    - CSV Files (*.csv)
    - Excel Files (*.xlsx, *.xls)
    - All Files (*.*)
  - 최근 사용한 디렉토리 표시

**Step 2: 파일 로드**
- 사용자 액션:
  - 파일 탐색 및 선택
  - [Open] 버튼 클릭
- 시스템 반응:
  1. StatusBar: "Loading file: [파일명]..." 표시
  2. ProgressBar 활성화
  3. 백그라운드에서 파일 파싱 (F-FILE-001)
  4. 데이터 검증 (F-VALID-001):
     - 파일 형식 검증
     - 필수 열(컬럼) 존재 확인
     - 데이터 타입 검증
     - 범위 검증

**Step 3: 데이터 적용**
- 유효한 파일:
  - 입력 필드에 데이터 자동 채우기
  - StatusBar: "File loaded: [파일명]" 표시
  - 사용자에게 자동 계산 여부 확인 다이얼로그:
    - "Would you like to calculate now?"
    - [Yes] [No]
  - [Yes] 선택 시 자동으로 계산 수행
- 유효하지 않은 파일:
  - 오류 다이얼로그 표시:
    - 오류 내용 상세 설명
    - 예상 파일 형식 안내
  - StatusBar: "Error: Failed to load file"

#### 지원 파일 형식

**CSV 형식 예시:**
```csv
Property,Value,Unit
Young's Modulus,200,GPa
Yield Strength,250,MPa
Tensile Strength,400,MPa
Width,10,mm
Thickness,5,mm
Length,50,mm
```

**Excel 형식:**
- Sheet 1: Material Properties
- 컬럼: Property | Value | Unit

---

### 2.4 시나리오 4: 결과 내보내기

#### 목표
계산된 결과와 그래프를 다양한 형식으로 내보냅니다.

#### 사용자 플로우
```
계산 완료 상태
  ↓
File > Export 메뉴 선택
  ↓
내보내기 옵션 선택:
  ├─ Export Graph as PNG/JPG/SVG
  ├─ Export Results as CSV/Excel
  └─ Generate PDF Report
  ↓
FileChooser 다이얼로그
  ↓
저장 위치 및 파일명 지정
  ↓
[Save] 버튼 클릭
  ↓
파일 저장
  ↓
완료 메시지 표시
```

#### 상세 인터랙션

**Option 1: 그래프 이미지 내보내기 (F-EXPORT-001)**
- 사용자 액션:
  - File > Export > Export Graph as PNG
  - 또는 VisualizationPanel의 [Export Graph] 버튼
- 시스템 반응:
  1. 이미지 형식 선택 다이얼로그 (PNG/JPG/SVG)
  2. FileChooser 표시
  3. 기본 파일명 제안: "stress_strain_curve_[날짜시간]"
  4. 이미지 품질 설정 (PNG: 300 DPI 이상, NF-QUAL-001)
  5. 파일 저장
  6. StatusBar: "Graph exported: [파일명]"
  7. 저장 완료 알림 다이얼로그:
     - "Graph saved successfully!"
     - [Open Folder] [OK]

**Option 2: 결과 데이터 내보내기 (F-FILE-002)**
- 사용자 액션:
  - File > Export > Export Results as CSV/Excel
  - 또는 ResultsPanel의 [Export Results] 버튼
- 시스템 반응:
  1. 파일 형식 선택 (CSV/Excel)
  2. FileChooser 표시
  3. 기본 파일명: "calculation_results_[날짜시간]"
  4. 데이터 포맷팅 및 저장
  5. StatusBar: "Results exported: [파일명]"

**Option 3: PDF 보고서 생성 (F-EXPORT-002)**
- 사용자 액션:
  - File > Export > Generate PDF Report
- 시스템 반응:
  1. 보고서 템플릿 선택 다이얼로그 (선택적)
  2. FileChooser 표시
  3. PDF 생성 진행:
     - 프로젝트 정보
     - 입력 데이터 요약
     - 응력-변형률 그래프 (고해상도)
     - 계산 결과 테이블
     - 메타데이터 (생성일시, 버전 등)
  4. ProgressBar로 진행 상황 표시
  5. 저장 완료 알림

---

### 2.5 시나리오 5: 그래프 인터랙션

#### 목표
사용자가 그래프를 확대, 축소, 이동하며 세부 데이터를 확인합니다.

#### 사용자 플로우
```
그래프 표시 상태
  ↓
다양한 인터랙션:
  ├─ 마우스 휠로 줌 인/아웃
  ├─ 드래그로 그래프 이동
  ├─ 영역 선택으로 확대
  ├─ 데이터 포인트 호버로 값 표시
  └─ 버튼으로 뷰 리셋
```

#### 상세 인터랙션

**줌 인/아웃**
- 마우스 휠:
  - 위로 스크롤: 줌 인 (10% 확대)
  - 아래로 스크롤: 줌 아웃 (10% 축소)
  - 마우스 커서 위치를 중심으로 줌
- 버튼:
  - [Zoom In] 버튼 (🔍+) 또는 Ctrl++
  - [Zoom Out] 버튼 (🔍-) 또는 Ctrl+-
  - 화면 중앙을 기준으로 줌
- 영역 선택 줌:
  - 마우스 좌클릭 + 드래그로 사각형 영역 선택
  - 선택 영역이 전체 화면에 맞춰 확대됨

**팬 (이동)**
- 마우스 우클릭 + 드래그
- 또는 Shift + 마우스 좌클릭 + 드래그
- 그래프가 드래그 방향으로 이동

**데이터 포인트 정보 표시**
- 마우스를 그래프 위에 호버
- 가장 가까운 데이터 포인트 하이라이트
- 툴팁으로 좌표값 표시:
  ```
  Strain: 0.125
  Stress: 312.5 MPa
  ```
- 주요 지점 (항복점, 인장강도) 마커는 항상 표시

**뷰 리셋**
- [Reset View] 버튼 (↺) 또는 Ctrl+0
- 그래프가 원래 크기와 위치로 복원
- 애니메이션 효과 (0.3초)

**그리드 토글**
- View > Toggle Grid 메뉴
- 그리드 선 표시/숨김

---

## 3. 이벤트 처리 흐름

### 3.1 버튼 클릭 이벤트

#### Calculate 버튼
```
사용자: [Calculate] 버튼 클릭
  ↓
MainFrame.onCalculateClicked()
  ↓
InputValidator.validateAll()
  ↓ (유효)
StatusBar.setMessage("Calculating...")
ProgressBar.setVisible(true)
CalculateButton.setEnabled(false)
  ↓
SwingWorker 시작
  ├─ doInBackground():
  │   ├─ Calculator.calculateProperties()
  │   └─ Calculator.generateCurve()
  └─ done():
      ├─ VisualizationPanel.updateGraph()
      ├─ ResultsPanel.updateTable()
      ├─ StatusBar.setMessage("Calculation completed")
      ├─ ProgressBar.setVisible(false)
      └─ CalculateButton.setEnabled(true)
```

**시간 제약**: 전체 프로세스 < 1초 (NF-PERF-001)

#### Reset 버튼
```
사용자: [Reset] 버튼 클릭
  ↓
MainFrame.onResetClicked()
  ↓
확인 다이얼로그:
  "Are you sure you want to reset all inputs?"
  [Yes] [No]
  ↓ (Yes)
InputPanel.clearAllFields()
VisualizationPanel.clearGraph()
ResultsPanel.clearTable()
StatusBar.setMessage("Ready")
```

---

### 3.2 메뉴 선택 이벤트

#### File > New
```
사용자: File > New (Ctrl+N)
  ↓
MainFrame.onNewProject()
  ↓
변경사항 확인:
  IF (hasUnsavedChanges) {
    다이얼로그: "Save current project?"
    [Save] [Don't Save] [Cancel]
      ↓ (Save)
      onSaveProject()
  }
  ↓
모든 데이터 초기화
StatusBar.setMessage("New project created")
```

#### File > Open
- 시나리오 3 참조

#### File > Save
```
사용자: File > Save (Ctrl+S)
  ↓
MainFrame.onSaveProject()
  ↓
IF (currentFile == null) {
  FileChooser 표시
  파일명 입력
}
  ↓
FileHandler.saveProject(currentFile)
  ↓
StatusBar.setMessage("Project saved: [파일명]")
```

---

### 3.3 입력 필드 이벤트

#### 실시간 입력 검증
```
사용자: InputField에 입력
  ↓
DocumentListener.insertUpdate()
  ↓
InputValidator.validateField(field)
  ↓
유효: 
  field.setBorder(normalBorder)
  field.setBackground(Color.WHITE)
무효:
  field.setBorder(errorBorder)
  field.setBackground(Color.LIGHT_RED)
  toolTip.show("Invalid input: [설명]")
```

#### 포커스 이탈 검증
```
사용자: InputField 포커스 이탈 (Tab, 클릭 등)
  ↓
FocusListener.focusLost()
  ↓
InputValidator.validateRange(field)
  ↓
범위 초과:
  경고 다이얼로그 표시
  "Value must be between [min] and [max]"
  field.selectAll()
  field.requestFocus()
```

---

## 4. 오류 처리 플로우

### 4.1 입력 오류

**유형 1: 비숫자 입력**
```
입력: "abc" in Young's Modulus field
  ↓
DocumentListener 감지
  ↓
입력 차단 (문자 입력 방지)
  ↓
비프음(beep) 재생
  ↓
toolTip: "Only numbers are allowed"
```

**유형 2: 범위 초과**
```
입력: 10000 GPa (허용 범위: 0-1000)
  ↓
FocusListener.focusLost()
  ↓
InputValidator.validateRange()
  ↓
다이얼로그:
  "Invalid Input"
  "Young's Modulus must be between 0 and 1000 GPa"
  "Current value: 10000 GPa"
  [OK]
  ↓
필드 하이라이트 및 포커스
```

**유형 3: 논리적 오류**
```
입력: σy (300 MPa) > σu (250 MPa)
  ↓
Calculate 버튼 클릭
  ↓
InputValidator.validateLogic()
  ↓
다이얼로그:
  "Logic Error"
  "Yield Strength (300 MPa) cannot exceed Tensile Strength (250 MPa)"
  [OK]
  ↓
문제 필드들 하이라이트
```

---

### 4.2 파일 오류

**유형 1: 파일을 찾을 수 없음**
```
File > Open
  ↓
파일 선택
  ↓
FileHandler.loadFile()
  ↓
FileNotFoundException
  ↓
다이얼로그:
  "File Not Found"
  "The selected file could not be found"
  "Path: [파일 경로]"
  [OK]
```

**유형 2: 잘못된 파일 형식**
```
파일 로드 시도
  ↓
파일 파싱
  ↓
InvalidFileFormatException
  ↓
다이얼로그:
  "Invalid File Format"
  "The file format is not supported"
  "Expected: CSV or Excel"
  "Received: [파일 형식]"
  [OK]
```

**유형 3: 데이터 불완전**
```
파일 파싱 성공
  ↓
데이터 검증
  ↓
필수 필드 누락 감지
  ↓
다이얼로그:
  "Incomplete Data"
  "Required fields are missing:"
  "- Young's Modulus"
  "- Yield Strength"
  "Do you want to manually input missing values?"
  [Yes] [No]
  ↓ (Yes)
누락된 필드로 포커스 이동
```

---

### 4.3 계산 오류

**유형 1: 수치 계산 오류**
```
Calculation 진행
  ↓
ArithmeticException (예: 0으로 나누기)
  ↓
SwingWorker.done()
  ↓
다이얼로그:
  "Calculation Error"
  "An error occurred during calculation"
  "Please check your input values"
  [OK]
  ↓
StatusBar: "Error: Calculation failed"
CalculateButton.setEnabled(true)
```

**유형 2: 메모리 부족**
```
Calculation 진행
  ↓
OutOfMemoryError
  ↓
다이얼로그:
  "Memory Error"
  "Insufficient memory to complete calculation"
  "Try reducing the data size or restart the application"
  [OK]
```

---

## 5. 피드백 메커니즘

### 5.1 시각적 피드백

#### 버튼 상태
- **기본 (Default)**:
  - 배경: Primary Color (#2196F3)
  - 텍스트: 흰색
  - 테두리: 없음
- **호버 (Hover)**:
  - 배경: 약간 어두운 파란색 (#1976D2)
  - 커서: 손가락 모양
- **클릭 (Pressed)**:
  - 배경: 더 어두운 파란색 (#0D47A1)
  - 시각적 눌림 효과
- **비활성 (Disabled)**:
  - 배경: 회색 (#BDBDBD)
  - 텍스트: 어두운 회색 (#757575)
  - 커서: 기본 화살표

#### 입력 필드 상태
- **기본**: 흰색 배경, 회색 테두리
- **포커스**: 파란색 테두리
- **유효한 입력**: 초록색 체크마크 아이콘
- **오류**: 빨간색 테두리, 연한 빨간색 배경
- **경고**: 노란색 테두리

#### 진행 상태
- **계산 중**:
  - ProgressBar 활성화
  - StatusBar 메시지 업데이트
  - 마우스 커서: 대기 중 (모래시계)
- **완료**:
  - ProgressBar 숨김
  - StatusBar: 성공 메시지 (초록색)
  - 완료 사운드 (선택적)

---

### 5.2 청각적 피드백 (선택적)

- **성공**: 짧은 "띵" 소리
- **오류**: 비프음(beep)
- **경고**: 중립적인 알림음

---

### 5.3 StatusBar 메시지

#### 메시지 유형 및 색상

| 상태 | 메시지 예시 | 색상 |
|------|-------------|------|
| Ready | "Ready" | 검정 |
| Working | "Calculating...", "Loading file..." | 파랑 |
| Success | "Calculation completed", "File saved" | 초록 |
| Warning | "Large file detected", "Unsaved changes" | 주황 |
| Error | "Error: Invalid input", "Failed to load file" | 빨강 |

#### 메시지 표시 시간
- 일반 메시지: 무기한 (다음 이벤트까지)
- 성공/경고 메시지: 3초 후 "Ready"로 자동 복구
- 오류 메시지: 사용자가 오류를 수정할 때까지 유지

---

## 6. 접근성 고려사항

### 6.1 키보드 네비게이션

#### Tab 순서
```
MaterialPropertiesPanel:
  YoungModulusField → YieldStrengthField → TensileStrengthField → ...
  ↓
SpecimenDimensionsPanel:
  WidthField → ThicknessField → LengthField → GaugeLengthField
  ↓
ControlPanel:
  CalculateButton → ResetButton → ImportDataButton
  ↓
PresetPanel:
  MaterialPresetComboBox → LoadPresetButton
```

#### 단축키 요약

| 단축키 | 기능 |
|--------|------|
| Ctrl+N | 새 프로젝트 |
| Ctrl+O | 파일 열기 |
| Ctrl+S | 저장 |
| Ctrl+E | 내보내기 |
| F5 | 계산 실행 |
| Ctrl+Z | 실행 취소 |
| Ctrl+Y | 다시 실행 |
| Ctrl++ | 줌 인 |
| Ctrl+- | 줌 아웃 |
| Ctrl+0 | 뷰 리셋 |
| F1 | 도움말 |
| Alt+F4 | 종료 |

---

### 6.2 툴팁 (Tooltip)

모든 인터랙티브 요소에 툴팁 제공:

**입력 필드**
- "Young's Modulus (E): 0-1000 GPa"
- "Yield Strength (σy): Must be less than Tensile Strength"

**버튼**
- "Calculate (F5): Run simulation"
- "Reset: Clear all inputs"

**메뉴 항목**
- "Export Graph: Save chart as image (PNG/JPG/SVG)"

---

### 6.3 화면 크기 대응

#### 최소 창 크기
- 너비: 1000px
- 높이: 700px

#### 반응형 동작
- **1000-1280px**: 기본 레이아웃
- **1280-1920px**: 여유 공간 활용 (그래프 확대)
- **1920px+**: 최대 크기 제한 (가독성 유지)

#### 크기 조절 시
- SplitPane: 비율 유지 (30:70)
- 그래프: 자동 리사이징 및 리페인트
- 폰트 크기: 고정 (스케일링 없음)

---

## 7. 성능 고려사항

### 7.1 응답 시간 목표

| 작업 | 목표 시간 | 최대 허용 시간 |
|------|-----------|----------------|
| 입력 검증 | 즉시 | 50ms |
| 계산 실행 | < 500ms | 1초 (NF-PERF-001) |
| 그래프 업데이트 | < 300ms | 500ms |
| 파일 로드 (< 1MB) | < 500ms | 2초 |
| 파일 로드 (1-10MB) | < 2초 | 5초 |
| 그래프 줌/팬 | 즉시 | 100ms |

---

### 7.2 백그라운드 처리

다음 작업은 SwingWorker로 백그라운드 처리:
- 계산 실행
- 파일 로드/저장
- PDF 보고서 생성
- 대용량 데이터 처리

---

### 7.3 메모리 관리

- 그래프 데이터 포인트: 최대 10,000개
- 초과 시 자동 샘플링 (균등 간격)
- 사용하지 않는 차트 객체 즉시 해제
- 이미지 캐싱 (최근 3개만 유지)

---

## 8. 사용자 경험 원칙

### 8.1 직관성 (Intuitiveness)
- 재료공학 용어 사용 (전공 학생에게 친숙)
- 논리적인 입력 순서 (위에서 아래로)
- 명확한 라벨링

### 8.2 일관성 (Consistency)
- 통일된 버튼 스타일
- 일관된 색상 사용 (오류=빨강, 성공=초록)
- 동일한 단축키 패턴

### 8.3 피드백 (Feedback)
- 모든 사용자 액션에 즉각적인 반응
- 진행 상태 명확히 표시
- 오류 발생 시 구체적인 설명 제공

### 8.4 용서 (Forgiveness)
- 실행 취소/다시 실행 기능
- 중요한 작업 전 확인 다이얼로그
- 입력 오류 시 자동 수정 제안

### 8.5 효율성 (Efficiency)
- 프리셋으로 빠른 입력
- 단축키 지원
- 자주 사용하는 기능 쉽게 접근

---

## 9. 테스트 시나리오

### 9.1 기능 테스트

#### Test Case 1: 기본 계산 수행
1. 애플리케이션 시작
2. 기본값 확인 (E=200 GPa, σy=250 MPa, σu=400 MPa)
3. [Calculate] 버튼 클릭
4. 그래프 표시 확인
5. ResultsTable 업데이트 확인
6. 계산 시간 < 1초 확인 ✓

#### Test Case 2: 프리셋 로드
1. PresetComboBox에서 "Aluminum" 선택
2. [Load Preset] 클릭
3. 물성값 자동 입력 확인:
   - E = 70 GPa
   - σy = 95 MPa
   - σu = 110 MPa
4. [Calculate] 클릭
5. 그래프 업데이트 확인 ✓

#### Test Case 3: 파일 불러오기
1. File > Open
2. sample_data.csv 선택
3. 데이터 자동 로드 확인
4. 자동 계산 다이얼로그에서 [Yes] 선택
5. 결과 표시 확인 ✓

---

### 9.2 오류 처리 테스트

#### Test Case 4: 잘못된 입력
1. YoungModulusField에 "abc" 입력 시도
2. 입력 차단 확인 ✓
3. YoungModulusField에 "10000" 입력
4. 포커스 이탈
5. 범위 초과 다이얼로그 표시 확인 ✓

#### Test Case 5: 논리적 오류
1. σy = 400 MPa 입력
2. σu = 300 MPa 입력
3. [Calculate] 클릭
4. 논리 오류 다이얼로그 표시 확인 ✓

---

### 9.3 UI/UX 테스트

#### Test Case 6: 그래프 인터랙션
1. 계산 수행하여 그래프 표시
2. 마우스 휠로 줌 인/아웃 테스트
3. 드래그로 그래프 이동 테스트
4. [Reset View] 버튼 클릭
5. 원래 뷰로 복원 확인 ✓

#### Test Case 7: 키보드 네비게이션
1. 첫 번째 입력 필드에 포커스
2. Tab 키로 모든 필드 순회
3. 순서 확인 ✓
4. F5 단축키로 계산 실행 확인 ✓

---

## 10. 참고 문서

- [MEVA 프로젝트 요구사항 정의서](https://www.notion.so/2a1da1be5cf48191a99dcd992794f840)
- [MainFrame GUI 설계 문서](https://www.notion.so/2a8da1be5cf481bd87d3f60aec360778)
- [GUI_Design_MainFrame.md](./GUI_Design_MainFrame.md)
- [system-requirements-specification.md](./system-requirements-specification.md)

---

## 11. 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 1.0 | 2025-11-17 | 초기 문서 작성 | MEVA 개발팀 |

---

**문서 끝**